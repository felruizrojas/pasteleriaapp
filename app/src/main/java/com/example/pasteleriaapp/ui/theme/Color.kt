package com.example.pasteleriaapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- TUS COLORES (Modo Claro) ---
val TituloMain = Color(0xFFD67BA8)         // --title-main (Rosa principal)
val TituloSecondary = Color(0xFF5AA58D)    // --title-secondary (Menta/Verde)
val TituloTertiary = Color(0xFFC4A35A)      // --title-tertiary (Dorado)
val PastelStrawberry = Color(0xFFF7B7D1)  // --pastel-strawberry (Rosa pálido)
val PastelMint = Color(0xFFBFE2D5)        // --pastel-mint (Menta pálido)
val Ink = Color(0xFF2A2A2A)                // --ink (Casi negro, para texto)
val InkMuted = Color(0xFF6C757D)          // --ink-muted (Gris, texto secundario)
val Surface = Color(0xFFFFFFFF)            // --surface (Blanco puro)
val SurfaceAlt = Color(0xFFFAFAFA)         // --surface-alt (Blanco hueso)

// --- Colores para Modo Oscuro ---
val DarkTituloMain = Color(0xFFE08DC0)      // Rosa un poco más brillante
val DarkInk = Color(0xFFF0F0F0)            // Texto claro
val DarkSurface = Color(0xFF1C1B1F)        // Fondo oscuro estándar M3
val DarkSurfaceAlt = Color(0xFF2A292D)    // Superficie oscura alternativa